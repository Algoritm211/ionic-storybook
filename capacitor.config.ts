import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-storybook',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
